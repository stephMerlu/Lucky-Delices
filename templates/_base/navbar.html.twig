<nav class="navbar navbar-expand-lg">
  <div class="container-fluid me-3 d-flex justify-content-between background-color: #eedc8f">
    <a class="navbar-brand me-2" href="{{ path('app_home') }}"><img src="/images/logo.jpeg" alt="Logo" style="width: 60px;"></a>    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-3 me-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_entrees') }}">Entrées</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_plats') }}">Plats</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_desserts') }}">Desserts</a>
        </li>
      </ul>
      <form class="d-flex justify-content-center" method="GET">
        <input class="form-control me-2" type="search" name="search" placeholder="Rechercher..." aria-label="Search">
        <input type="hidden" name="searchBy" value="name">
        <input type="hidden" name="searchBy" value="ingredient">
        <button class="btn mt-2" type="submit" style="background-image: url('/images/search.png'); background-repeat: no-repeat;" formaction="{{ path('app_search') }}"></button>
      </form>
      <ul class="navbar-nav ms-auto mt-2 mt-lg-0">
        {% if app.user %}
          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
              <button class="btn btn-outline-my-custom me-2 btn-signup mt-2 mt-lg-0"><a href="{{ path('index') }}" style="text-decoration: none; color: inherit;">Administration</a></button>
            </li>
          {% endif %}
          <li class="nav-item">
            <button class="btn btn-outline-my-custom me-2 btn-signup mt-2 mt-lg-0"><a href="{{ path('app_user_profile') }}" style="text-decoration: none; color: inherit;">Mon compte</a></button>
          </li>
          <li class="nav-item">
            <button class="btn btn-my-custom btn-login mt-2 mt-lg-0"><a href="{{ path('app_logout') }}" style="text-decoration: none; color: inherit;">Me déconnecter</a></button>
          </li>
        {% else %}
          <li class="nav-item">
            <button class="btn btn-outline-my-custom me-2 btn-signup mt-2 mt-lg-0"><a href="{{ path('app_register') }}" style="text-decoration: none; color: inherit;">S'inscrire</a></button>
          </li>
          <li class="nav-item">
            <button class="btn btn-my-custom btn-login mt-2 mt-lg-0"><a href="{{ path('app_login') }}" style="text-decoration: none; color: inherit;">Se connecter</a></button>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>


