{{ form_start(form) }}
    {{ form_row(form.name, {'label_attr': {'class': 'my-custom-label'}, 'attr': {'class': 'mb-3 ms-2 form-control'}}) }}
    {{ form_row(form.recipes, {'label_attr': {'class': 'my-custom-label'}, 'attr': {'class': 'mb-3 ms-2'}}) }}
    <div class="text-center">
        <button class="btn btn-add active mt-2">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
