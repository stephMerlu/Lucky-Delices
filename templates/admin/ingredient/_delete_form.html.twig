<form method="post" class="text-end" action="{{ path('app_admin_ingredient_delete', {'id': ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
    <button class="btn btn-danger mt-1 mb-2 float-end">Supprimer</button>
</form>
