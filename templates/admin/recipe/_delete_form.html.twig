<form method="post" action="{{ path('app_admin_recipe_delete', {'id': recipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe.id) }}">
        <div class="text-right">
            <button class="btn btn-danger mt-1 mb-2 float-end">
                Supprimer
            </button>
        </div>
    </form>
    